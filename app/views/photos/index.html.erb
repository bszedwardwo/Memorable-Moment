<% if @photo.exists? %>
  <% @photo.each do |photo| %>
    <%= image_tag photo.image.variant(resize: '500x500') %>
    <%# if photo.comment.exists? %>
      <%# photo.comment %>
    <%# end %>
  <% end %>
<% end %>