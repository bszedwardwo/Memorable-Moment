<div class="contents row">
  <div class="container">
    <h3>投稿する</h3>
    <%= form_with(model: @photo, local: true) do |form| %>
      <%= form.file_field :image %>
      <%= form.submit "SEND" %>
    <% end %>
  </div>
</div>